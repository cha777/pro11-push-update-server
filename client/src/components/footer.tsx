import {} from 'react-bootstrap';

const Footer = () => {
  return <footer className='my-5 pt-5 text-muted text-center text-small'>© {new Date().getFullYear()} DirectFN</footer>;
};

export default Footer;
